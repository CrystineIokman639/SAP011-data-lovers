// estas funciones son de ejemplo

export const example = () => {
  return 'example';
};

export const anotherExample = () => {
  return 'OMG';
};

// function especieFilter(results){
// const speciesResults = results.filter(results =>(results === "species"));
// console.log( speciesResults );

// const filterHuman = speciesResults.map(speciesResults => `${speciesResults.Human}` + "Human" );
// console.log( filterHuman );


// };

 
